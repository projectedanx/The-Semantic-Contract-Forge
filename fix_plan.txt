1. Modify `services/geminiService.ts` to remove the hardcoded API key implementation. Accept the `apiKey` string in `validatePromptOutput` and `generateRole`. Instantiate the `GoogleGenerativeAI` model within these functions.
2. Use `cat services/geminiService.ts` to verify the changes were applied correctly to `geminiService.ts`.
3. Modify `App.tsx` to add `apiKey` state initialized from `localStorage`. Update the UI to include a password input field for the `apiKey` inside the `<main>` container before `<TierSelector>`. Modify `handleValidate` to pass `apiKey` to `validatePromptOutput`. Pass `apiKey` down as a prop to `<PromptEditor>`.
4. Use `cat App.tsx` to verify the changes were applied correctly to `App.tsx`.
5. Modify `components/PromptEditor.tsx` to accept the new `apiKey` prop and pass it to `generateRole` using an inline arrow function: `onGenerate={(persona) => generateRole(persona, apiKey)}`.
6. Use `cat components/PromptEditor.tsx` to verify the changes were applied correctly to `components/PromptEditor.tsx`.
7. Modify `services/__tests__/geminiService.test.ts` to pass a dummy `apiKey` string (e.g., `'dummy_key'`) to `validatePromptOutput` and `generateRole` in all test cases.
8. Use `cat services/__tests__/geminiService.test.ts` to verify the changes were applied correctly to the test file.
9. Run `export VITE_GEMINI_API_KEY="dummy" && npm test` to ensure all tests pass and functionality is intact.
10. Remove `VITE_GEMINI_API_KEY` instructions from `README.md` and `.env.example`.
11. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
12. Submit the fix with a descriptive commit message.
